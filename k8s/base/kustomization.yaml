apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml

configMapGenerator:
  - name: cm-env
    literals:
      - FLASK_APP=flasky.py
      - FLASK_ENV=production
